// import { useState } from "react";
// import { useMutation } from "@tanstack/react-query";
// import toast from "react-hot-toast";
// import { updateCompanySubscription } from "../../../../shared/services/apiCompany";

export function useUpdateSubscription(/* companyId: number */) {
  // const [form, setForm] = useState({
  //   is_annual: false,
  //   subscription_id: null,
  // });

  // const mutation = useMutation({
  //   mutationFn: () => updateCompanySubscription(form, companyId),
  //   onSuccess: () => {
  //     toast.success("Your subscription plan has been updated successfully!");
  //   },
  //   onError: (error) => {
  //     toast.error("Failed to update subscription plan: " + error.message);
  //   },
  // });

  // const handleUpdatePlan = (newPlan) => {
  //   setForm({
  //     is_annual: newPlan.is_annual,
  //     subscription_id: newPlan.id,
  //   });
  //   mutation.mutate();
  // };

  // return { handleUpdatePlan };
  return;
}
